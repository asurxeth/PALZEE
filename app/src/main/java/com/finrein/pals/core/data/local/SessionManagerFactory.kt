package com.finrein.pals.core.data.local

import android.content.Context

/**
 * Synthetic constructor function for Android to maintain 100% source-compatibility
 * with callers that instantiate SessionManager using a Context.
 */
fun SessionManager(context: Context): SessionManager {
    return SessionManager()
}
